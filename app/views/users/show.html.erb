<% provide(:title, @user.username) %>
<div class="row">
    <aside class="col-md-4">
        <section class="user_info">
            <h1>
                <%= gravatar_for @user %>
                <%= @user.username %>
                <%= "Fullname:" %> <%= current_user.fullname %><br>
                <%= "Gender:" %> <%= current_user.gender %><br>
                <%= "Contact:" %> <%= current_user.contact %>
            </h1>
        </section>
    </aside>
</div>
<% unless @join.empty? %>
  <h2>List of works joined</h2>
  <table class="table table-border table-hover">
      <thead>
          <tr>
              <th>Work's name</th>
              <th>Skill Require</th>
              <th>Customer's name</th>
              <th>Work's price</th>
          </tr>
      </thead>
      <tbody>
          <% @join.each do |f|  %>
